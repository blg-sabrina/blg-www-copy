name: Deploy BigLedger Website to S3

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Setup Hugo
        run: |
          wget https://github.com/gohugoio/hugo/releases/download/v0.147.9/hugo_extended_withdeploy_0.147.9_linux-amd64.tar.gz
          tar -xzf hugo_extended_withdeploy_0.147.9_linux-amd64.tar.gz
          sudo mv hugo /usr/local/bin/
          hugo version

      - name: Build Hugo site
        run: hugo --minify

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to S3 and Invalidate CloudFront
        run: |
          # Hugo deploy handles both S3 upload and CloudFront invalidation.
          # --force re-uploads every object so the Cache-Control headers from
          # the `matchers` block in hugo.yaml are applied to all files, not
          # just the ones whose content changed since the last deploy.
          echo "Deploying to S3 and invalidating CloudFront distribution ELHXIGVKWA8XV"
          hugo deploy --target=production --maxDeletes -1 --invalidateCDN --force
          
      - name: Verify CloudFront Invalidation
        if: success()
        run: |
          echo "Checking recent CloudFront invalidations..."
          aws cloudfront list-invalidations \
            --distribution-id ELHXIGVKWA8XV \
            --max-items 3 \
            --query 'InvalidationList.Items[0].[Id,Status,CreateTime]' \
            --output table
          echo "✅ Deployment and invalidation completed"

      - name: Verify deployment
        run: |
          echo "Site deployed to: https://www.bigledger.com"
          echo "Checking if site is accessible..."
          curl -I https://www.bigledger.com || true